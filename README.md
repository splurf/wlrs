# wlrs

- Minecraft Authenticated Whitelister

#### Build
```bash
cargo build --release
trunk build --release
```

#### Usage
```bash
cargo run --release    # backend
trunk serve --release  # fronend
